usage: arbitrary_analyzer [-h] [-g PATTERN] [--num-windows NUM_WINDOWS]
                          [--len-fft LEN_FFT] [-w WINDOW]
                          [-r RESPONSE_PATTERN] [-c CALIBRATION_SIGNAL_FILE]
                          [-i CALIBRATION_RESPONSE_FILE] [-d DELAY_START]
                          [--discard-s DISCARD_START_S DISCARD_END_S]
                          [-b MIN_FREQUENCY_HZ MAX_FREQUENCY_HZ]
                          [-t MAX_AMPLITUDE_PERCENT MAX_PHASE_DEGREES]
                          [-m ORIENTATION_MAP ORIENTATION_MAP]
                          [--ims-instrument-type IMS_INSTRUMENT_TYPE]
                          [--calper CALPER] [--fit N]
                          [--out-of-band FACTOR_1 FACTOR_2]
                          [-p {none,basic,diagnostic,all}] [--width WIDTH]
                          [--fontsize FONTSIZE] [--dpi DPI]
                          [--fmt {PDF,PNG,SVG,EPS}] [-v]

Analyzer for broadband calibrations of seismometers using arbitrary signals.

Calibration output waveforms, station metadata and calibration input response
metadata must be provided.

## Notes

1. Transfer function model terminology:
  * `sensor`: first stage from ground motion (m/s or m/s^2) to V
  * `cal`: all other stages, from V to ground motion, typically m/s^2
  * `system` product of `sensor` and `cal`, dimensionless and typically
    having a slope proportional to 1/f in the passband.
2. Splitting of response stages:
  * `datalogger`: defined as stages including and after counts/V transition
    * datalogger stages are considered flat with respect to frequency
    * datalogger preamps are considered part of the sensor
  * `sensor`: stanges going from ground motion to V
    * user specified `n` permits control over inclusion of preamps in fitting
3. The length of FFT segments can be specified directly, and is efficient
   for any number with a large number of small factors (not just powers
   of 2), but it is generally more convenient to specify a target number
   of segments as it is the (actual) number of segments in Welch's method
   which reduces the variance in the result.
4. Not all of the calibration input response metadata is used. In particular,
   network, station, location and channel codes are ignored. Only the first two
   stages of the response are retained. The first is assumed to be the sensor
   poles and zeros, while the second gives the digitial-to-analog conversion
   sensitivity.

## References

Ackerley, N., & Gias, Z. (2026, in review). Modeling and experimental
verification of the temperature dependence of short-period seismometer
response, using pole-zero fitting, Seismol. Res. Lett.
https://doi.org/10.1785/0220260110

options:
  -h, --help            show this help message and exit
  -g PATTERN, --pattern PATTERN
                        glob pattern matching calibration output files, in any
                        format readable by obspy.read() (default: *.mseed)
  --num-windows NUM_WINDOWS
                        number of windows to used for Welch's method (default:
                        20)
  --len-fft LEN_FFT     window length for Welch's method, overrides
                        num_windows (default: None)
  -w WINDOW, --window WINDOW
                        window function to be used for Welch's method
                        (default: hann)
  -r RESPONSE_PATTERN, --response-pattern RESPONSE_PATTERN
                        station metadata in any format readable by
                        obspy.read_inventory() (default: *.resp)
  -c CALIBRATION_SIGNAL_FILE, --calibration-signal-file CALIBRATION_SIGNAL_FILE
                        calibration input file, lzma compressed (default:
                        prb_1V_10ms_3h.lzma)
  -i CALIBRATION_RESPONSE_FILE, --calibration-response-file CALIBRATION_RESPONSE_FILE
                        calibration input response in any format readable by
                        obspy.read_inventory(). (default:
                        Centaur_Trillium120Q_Calibration.xml)
  -d DELAY_START, --delay-start DELAY_START
                        amount to delay calibration start time, in seconds
                        (default: 0)
  --discard-s DISCARD_START_S DISCARD_END_S
                        duration to discard from start and end, in seconds
                        (default: (0, 0))
  -b MIN_FREQUENCY_HZ MAX_FREQUENCY_HZ, --test-band-hz MIN_FREQUENCY_HZ MAX_FREQUENCY_HZ
                        frequency band, in Hz, over which to apply test limits
                        (default: (0.02, 16))
  -t MAX_AMPLITUDE_PERCENT MAX_PHASE_DEGREES, --test-limits MAX_AMPLITUDE_PERCENT MAX_PHASE_DEGREES
                        maximum deviation from nominal of amplitude in percent
                        and phase in degrees (default: (5, 5))
  -m ORIENTATION_MAP ORIENTATION_MAP, --orientation-map ORIENTATION_MAP ORIENTATION_MAP
                        Map external channel orientation to internal axis
                        orientations (default: ('', ''))
  --ims-instrument-type IMS_INSTRUMENT_TYPE
                        if provided, write IMS2.0 CALIBRATE_RESULT message
                        with type (default: )
  --calper CALPER       calibration period in seconds. Overrides
                        stage_gain_frequency given in response file. (default:
                        None)
  --fit N               fit the first N stages of the sensor response poles
                        and zeros (default: 0)
  --out-of-band FACTOR_1 FACTOR_2
                        fix poles and zeroes more than this factor beyond
                        available frequency range (default: (2, 5))
  -p {none,basic,diagnostic,all}, --plot {none,basic,diagnostic,all}
                        generate basic (start & end check, transfer function
                        and variance) or diagnostic plots for each calibration
                        (default: none)
  --width WIDTH         width of plots in inches, ignored for start/end check
                        plots (default: 4.25)
  --fontsize FONTSIZE   font size to be used in plots, in typographic points
                        (default: 8)
  --dpi DPI             resolution to use for plots in dots per inch (default:
                        150)
  --fmt {PDF,PNG,SVG,EPS}
                        output file format for plots (default: png)
  -v, --version         show program's version number and exit
