# Sine Calibration Examples

This folder contains the input files and reusults of running the following command in this folder:
```bash
sine_analyzer --plot
```

For more help and options, see:
```bash
sine_analyzer --help
```
